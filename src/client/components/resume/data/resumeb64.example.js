/*
 * Save your resume as a PDF.
 * Base64 encode the PDF file. There are plenty of online tools for this, so Google it.
 * Paste the encoded data into the string `rb64` in this file.
 * Rename this file `resumeb64.js`
 */

const rb64 = ``;

export default rb64;
